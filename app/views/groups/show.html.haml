- title "#{@group.name} Group"

%h1= "View #{@group.name.camelcase} Group"

%table
  %thead
    %th Username
    %th Green Miles
  - @group.users.by_green_miles.each do |user|
    %tr
      %td= user.username
      %td= user.green_miles
